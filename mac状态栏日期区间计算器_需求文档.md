# macOS 状态栏日期区间计算器需求文档

## 1. 产品定位

本产品是一个 macOS 状态栏日期区间计算工具。用户点击菜单栏图标后，在下方弹出轻量日历面板，通过鼠标拖拽或点击选择日期范围，实时计算总天数、工作日、休息日、法定节假日、调休工作日，并支持农历显示、中国国内放假提示、自定义工作规则和主题切换。

产品主要服务于办公排期、项目周期评估、请假天数计算、交付日期参考、合同周期核算等场景。

## 2. 核心目标

1. 用户可以在 macOS 状态栏快速打开日历面板。
2. 用户可以通过鼠标框选日期范围，并支持跨月份、跨年份选择。
3. 系统实时显示所选范围内的总天数、实际工作日、休息日、节假日、调休工作日。
4. 日历格子内显示公历日期、农历日期、节日名称、休息标记、补班标记。
5. 支持绿色清新主题和粉色可爱主题。
6. 所有设置与历史记录本地保存。
7. 优先保证结果准确、操作轻量、界面清晰。

## 3. 目标用户

| 用户类型 | 使用场景 |
|---|---|
| 上班族 | 计算请假、调休、排班、项目周期 |
| 项目经理 | 估算项目工期、交付周期、迭代周期 |
| 行政人事 | 查看节假日、补班日、休息日 |
| 自由职业者 | 计算合作周期、交付天数 |
| 开发者和设计师 | 计算需求排期和版本周期 |

## 4. 主要使用场景

### 4.1 项目排期

用户选择一个项目开始日期和结束日期，系统立即展示：

```text
2026.05.02 至 2026.05.31
共 30 天
实际工作日 22 天
休息日 8 天
法定节假日 0 天
调休工作日 1 天
```

### 4.2 请假计算

用户选择请假日期范围，系统区分自然日、工作日和休息日，方便判断实际请假天数。

### 4.3 节假日查看

用户切换到指定月份，直接看到休息标记、补班标记、农历日期和节日名称。

### 4.4 跨月周期计算

用户从本月拖拽到下个月，或者通过点击起止日期跨月份选择，系统持续高亮区间并实时统计。

## 5. 功能范围

| 模块 | 功能 |
|---|---|
| 状态栏入口 | 常驻菜单栏，点击弹出日历 |
| 日历视图 | 单月、双月、三月视图 |
| 日期选择 | 拖拽框选、点击起止、快捷选择 |
| 跨月选择 | 支持跨月、跨年 |
| 日期统计 | 总天数、工作日、休息日、节假日、调休工作日 |
| 农历显示 | 简洁模式、完整模式、关闭 |
| 中国节假日 | 法定节假日、调休工作日、节日名称 |
| 工作规则 | 双休、单休、大小周、自定义每周规则 |
| 自定义日期 | 单独设置某天为工作日或休息日 |
| 复制结果 | 多种复制模板，自定义模板 |
| 历史记录 | 保存最近选择的日期范围 |
| 主题系统 | 绿色清新、粉色可爱 |
| 设置中心 | 视图、主题、工作规则、节假日、日期格式 |
| 本地存储 | UserDefaults、SwiftData 或本地 JSON |

## 6. 视图模式

### 6.1 单月视图

适合窗口较小的状态栏场景。

要求：

1. 显示当前月份完整日历。
2. 补齐上个月和下个月的空白日期。
3. 支持在当前月份内选择日期范围。
4. 可以通过左右箭头切换月份。
5. 支持点击年份和月份下拉选择。

### 6.2 双月视图

作为默认推荐视图。

要求：

1. 同时显示当前月份和下一个月份。
2. 支持从左侧月份拖拽到右侧月份。
3. 适合跨月排期。
4. 面板宽度需要大于单月视图。

### 6.3 三月视图

用于宽屏用户。

要求：

1. 同时显示三个月。
2. 适合长周期排期。
3. 可以在设置中开启，默认关闭。

## 7. 日期选择规则

### 7.1 拖拽框选

用户在某一天按下鼠标左键，然后拖动到另一天，释放鼠标后完成选择。

交互要求：

1. 鼠标按下时记录开始日期。
2. 鼠标拖动经过日期时实时更新结束日期。
3. 选中范围实时高亮。
4. 底部统计实时更新。
5. 支持从后往前拖拽。
6. 如果拖拽到面板边缘，可以自动切换月份。
7. 释放鼠标后保存最终选择结果。

### 7.2 点击起止日期

用户第一次点击设置开始日期，第二次点击设置结束日期。

规则：

1. 第一次点击为开始日期。
2. 第二次点击为结束日期。
3. 如果结束日期早于开始日期，系统自动交换开始和结束。
4. 第三次点击重新开始新的选择。
5. 点击已选范围外的日期时，重新进入选择状态。

### 7.3 快捷选择

需要内置以下快捷选择：

| 快捷项 | 规则 |
|---|---|
| 今天 | 选择当天 |
| 本周 | 周一至周日 |
| 本月 | 本月第一天至本月最后一天 |
| 下月 | 下月第一天至下月最后一天 |
| 未来 7 天 | 从今天开始连续 7 天 |
| 未来 14 天 | 从今天开始连续 14 天 |
| 未来 30 天 | 从今天开始连续 30 天 |
| 本季度 | 当前季度第一天至最后一天 |
| 自定义输入 | 用户手动输入开始日期和结束日期 |

## 8. 日期计算规则

### 8.1 总天数

总天数必须包含首尾日期。

示例：

| 日期范围 | 总天数 |
|---|---:|
| 2026.05.01 至 2026.05.01 | 1 |
| 2026.05.01 至 2026.05.02 | 2 |
| 2026.05.01 至 2026.05.31 | 31 |

### 8.2 工作日

工作日根据以下优先级计算：

| 优先级 | 规则 |
|---:|---|
| 1 | 用户自定义工作日 |
| 2 | 用户自定义休息日 |
| 3 | 中国调休工作日 |
| 4 | 中国法定节假日 |
| 5 | 工作规则，包含双休、单休、大小周、自定义每周规则 |
| 6 | 默认周一到周五为工作日 |

说明：

1. 自定义日期优先级最高。
2. 调休工作日需要覆盖周末规则。
3. 法定节假日需要覆盖普通工作日规则。
4. 用户开启自定义规则时，应以自定义规则为主。

### 8.3 休息日

休息日包含：

1. 周末休息日。
2. 法定节假日。
3. 用户自定义休息日。
4. 根据单休、大小周、自定义每周规则计算出的休息日。

### 8.4 法定节假日

法定节假日来自本地节假日数据。

要求：

1. 每年一份节假日配置。
2. 配置内包含节假日和调休工作日。
3. 支持手动导入。
4. 支持本地编辑。
5. 如果某年份无数据，需要在 UI 中明确提示。
6. 不能把无数据年份错误地按中国放假规则计算。

### 8.5 调休工作日

调休工作日需要显示“班”标记，并按工作日计算。

示例：

```json
{
  "date": "2026-05-09",
  "name": "劳动节调休上班",
  "type": "workday"
}
```

## 9. 农历显示

### 9.1 显示模式

| 模式 | 说明 |
|---|---|
| 关闭 | 不显示农历 |
| 简洁 | 显示农历初一、十五、节日和节气 |
| 完整 | 每天显示农历日期 |

默认建议使用完整模式。

### 9.2 显示规则

1. 每个日期格子中，公历日期为主。
2. 农历日期显示在公历日期下方。
3. 农历字体小于公历日期。
4. 农历初一显示月份名称，例如“五月初一”。
5. 普通农历日期显示“初二”“廿三”等。
6. 节气和传统节日可以替换农历日期显示，例如“立夏”“端午节”。
7. 如果同一天有公历节日、农历节日、节气，显示优先级需要固定。

### 9.3 农历文案优先级

| 优先级 | 类型 | 示例 |
|---:|---|---|
| 1 | 法定节假日名称 | 劳动节 |
| 2 | 重要传统节日 | 春节、中秋节、端午节 |
| 3 | 节气 | 立夏、小满 |
| 4 | 公历节日 | 母亲节、儿童节 |
| 5 | 农历日期 | 初二、廿三 |

## 10. 中国国内放假提示

### 10.1 日期标记

| 类型 | 标记 | 显示位置 |
|---|---|---|
| 法定节假日 | 休 | 日期格右上角 |
| 调休工作日 | 班 | 日期格右上角 |
| 今天 | 描边或圆点 | 日期数字区域 |
| 当前选中起点 | 实色圆角块 | 日期格 |
| 当前选中终点 | 实色圆角块 | 日期格 |
| 选中区间 | 半透明背景 | 日期格 |

### 10.2 颜色建议

| 类型 | 绿色主题 | 粉色主题 |
|---|---|---|
| 休 | 绿色或粉色小圆角标 | 粉色小圆角标 |
| 班 | 蓝灰色小圆角标 | 蓝灰色小圆角标 |
| 节日文字 | 主色 | 主色 |
| 农历文字 | 次级文字 | 次级文字 |
| 非本月日期 | 浅灰 | 浅灰 |

## 11. 工作规则

### 11.1 标准双休

规则：

1. 周一到周五为工作日。
2. 周六周日为休息日。
3. 中国节假日和调休规则优先。

### 11.2 单休模式

规则：

1. 周一到周六为工作日。
2. 周日为休息日。
3. 可设置周六或周日为固定休息日。

### 11.3 大小周

规则：

1. 大周为双休。
2. 小周为单休。
3. 用户需要设置起始周日期。
4. 用户需要设置起始周是大周或小周。
5. 系统按周循环计算。

示例：

```text
起始周：2026.05.04 所在周
起始周类型：大周
大周：周六、周日休息
小周：周日休息
```

### 11.4 自定义每周规则

用户可以选择每周哪些天是工作日。

示例：

```text
周一 工作
周二 工作
周三 工作
周四 工作
周五 工作
周六 休息
周日 休息
```

## 12. 复制结果

### 12.1 内置复制模板

| 模板 | 示例 |
|---|---|
| 简洁版 | 2026.05.02 至 2026.05.31，共 30 天 |
| 工作版 | 2026.05.02 至 2026.05.31，共 30 天，实际工作日 22 天，休息日 8 天 |
| 请假版 | 请假范围：2026.05.02 至 2026.05.31，需请假工作日 22 天 |
| 项目排期版 | 项目周期：2026.05.02 至 2026.05.31，总计 30 天，其中工作日 22 天 |
| Markdown 表格 | 日期范围、总天数、工作日、休息日 |
| JSON | 输出结构化数据 |

### 12.2 JSON 复制格式

```json
{
  "startDate": "2026-05-02",
  "endDate": "2026-05-31",
  "totalDays": 30,
  "workdays": 22,
  "restDays": 8,
  "holidays": 0,
  "adjustedWorkdays": 1
}
```

### 12.3 自定义模板变量

支持以下变量：

| 变量 | 含义 |
|---|---|
| `{startDate}` | 开始日期 |
| `{endDate}` | 结束日期 |
| `{totalDays}` | 总天数 |
| `{workdays}` | 工作日 |
| `{restDays}` | 休息日 |
| `{holidays}` | 法定节假日 |
| `{adjustedWorkdays}` | 调休工作日 |
| `{dateRange}` | 日期范围文本 |
| `{lunarStart}` | 开始日期农历 |
| `{lunarEnd}` | 结束日期农历 |

## 13. 历史记录

### 13.1 保存规则

1. 每次完成日期范围选择后，自动保存到历史记录。
2. 默认保存最近 20 条。
3. 用户可以在设置中调整为 10、20、50 条。
4. 重复范围可以合并，也可以按选择时间分别保存，由设置决定。

### 13.2 历史记录字段

```json
{
  "id": "uuid",
  "startDate": "2026-05-02",
  "endDate": "2026-05-31",
  "totalDays": 30,
  "workdays": 22,
  "restDays": 8,
  "createdAt": "2026-07-02T14:00:00+08:00",
  "note": "项目一期排期"
}
```

## 14. 设置项

| 设置项 | 选项 |
|---|---|
| 默认视图 | 单月、双月、三月 |
| 默认主题 | 绿色清新、粉色可爱、跟随系统 |
| 农历显示 | 关闭、简洁、完整 |
| 中国放假提示 | 开启、关闭 |
| 调休工作日提示 | 开启、关闭 |
| 起始星期 | 周一、周日 |
| 工作规则 | 双休、单休、大小周、自定义 |
| 日期格式 | yyyy.MM.dd、yyyy/MM/dd、yyyy年M月d日 |
| 复制模板 | 内置模板或自定义 |
| 历史记录数量 | 10、20、50 |
| 开机启动 | 开启、关闭 |
| 状态栏图标 | 日历、叶子、猫爪 |

## 15. 数据模型建议

### 15.1 日期单元

```swift
struct CalendarDay: Identifiable {
    let id: String
    let date: Date
    let isCurrentMonth: Bool
    let isToday: Bool
    let weekday: Int
    let solarDay: Int
    let lunarText: String?
    let festivalText: String?
    let holidayType: HolidayType?
    let isSelected: Bool
    let isSelectionStart: Bool
    let isSelectionEnd: Bool
}
```

### 15.2 节假日类型

```swift
enum HolidayType: String, Codable {
    case holiday
    case adjustedWorkday
    case customRestDay
    case customWorkday
    case none
}
```

### 15.3 日期范围

```swift
struct DateRangeSelection: Codable {
    var startDate: Date
    var endDate: Date
}
```

### 15.4 统计结果

```swift
struct DateStats: Codable {
    var totalDays: Int
    var workdays: Int
    var restDays: Int
    var holidays: Int
    var adjustedWorkdays: Int
    var customRestDays: Int
    var customWorkdays: Int
}
```

### 15.5 节假日配置

```swift
struct HolidayConfig: Codable {
    var year: Int
    var region: String
    var holidays: [HolidayItem]
    var adjustedWorkdays: [HolidayItem]
}

struct HolidayItem: Codable {
    var date: String
    var name: String
    var type: String
}
```

## 16. 本地存储

| 数据 | 推荐存储 |
|---|---|
| 普通设置 | UserDefaults |
| 主题设置 | UserDefaults |
| 历史记录 | SwiftData 或本地 JSON |
| 节假日配置 | App Bundle 内置 JSON 加用户本地 JSON |
| 自定义日期 | SwiftData 或本地 JSON |
| 复制模板 | UserDefaults 或本地 JSON |

## 17. 技术建议

| 功能 | macOS 技术 |
|---|---|
| 状态栏入口 | NSStatusItem |
| 弹出面板 | NSPopover 或 NSPanel |
| UI 框架 | SwiftUI |
| 日期计算 | Foundation Calendar |
| 农历转换 | Calendar.Identifier.chinese 或本地农历库 |
| 数据存储 | UserDefaults、SwiftData、本地 JSON |
| 复制 | NSPasteboard |
| 开机启动 | ServiceManagement LoginItem |
| 快捷键 | KeyboardShortcuts 或自定义监听 |
| 菜单栏图标 | SF Symbols 或自定义模板图标 |

## 18. 异常处理

| 场景 | 处理 |
|---|---|
| 节假日数据缺失 | 显示“当前年份暂无节假日数据” |
| 农历转换失败 | 隐藏农历文字，不影响公历显示 |
| 日期范围过大 | 仍可计算，但可提示计算范围较长 |
| 用户切换年份 | 自动加载对应年份节假日数据 |
| 自定义规则冲突 | 按优先级覆盖，并在设置页提示 |
| 本地 JSON 损坏 | 回退默认配置，并提示用户重新导入 |

## 19. 验收标准

1. 点击 macOS 状态栏图标后，日历面板可以在菜单栏下方弹出。
2. 日历能正确显示当前月份、公历日期、农历日期。
3. 周六周日有明显区分。
4. 法定节假日显示“休”。
5. 调休工作日显示“班”。
6. 用户可以拖拽选择日期范围。
7. 用户可以点击起止日期选择范围。
8. 选择范围支持跨月和跨年。
9. 统计结果包含总天数、工作日、休息日、法定节假日、调休工作日。
10. 总天数必须包含首尾日期。
11. 中国节假日和调休规则优先于普通周末规则。
12. 绿色主题和粉色主题都可切换。
13. 设置项可以本地保存。
14. 复制结果可以写入系统剪贴板。
15. 历史记录可以保存、恢复和删除。
