# macOS 状态栏日期区间计算器 UI 详细设计文档

## 1. 设计目标

本 UI 设计用于一个 macOS 状态栏日期区间计算器。整体风格需要轻量、清晰、适合高频办公使用，同时提供绿色清新主题和粉色可爱主题。

设计需要参考 macOS 系统日历的清晰信息层级，并加入农历、休息标记、补班标记、日期范围选择、底部统计卡片等功能。界面需要支持状态栏弹出面板，面板从菜单栏图标下方展开，顶部带有小三角指向菜单栏图标。

## 2. 整体布局

### 2.1 面板结构

主面板由五部分组成：

```text
菜单栏图标
   ▲
状态栏弹出面板
├─ 顶部工具栏
├─ 星期栏
├─ 日历网格
├─ 日期统计区
└─ 底部操作区或扩展按钮
```

### 2.2 默认窗口尺寸

| 视图 | 宽度 | 高度 |
|---|---:|---:|
| 单月视图 | 720 px | 680 px |
| 双月视图 | 1040 px | 720 px |
| 三月视图 | 1320 px | 740 px |

如果实际开发中希望更轻量，单月状态栏面板可以缩小到 420 px 至 520 px 宽，但需要保证农历和休息标记可读。

### 2.3 面板圆角和阴影

| 属性 | 数值 |
|---|---:|
| 面板圆角 | 18 px |
| 外层描边 | 1 px |
| 阴影模糊 | 24 px |
| 阴影透明度 | 0.14 |
| 顶部小三角宽度 | 18 px |
| 顶部小三角高度 | 10 px |

## 3. 顶部工具栏设计

### 3.1 布局

顶部工具栏高度建议为 72 px。

从左到右：

```text
[工作日模式 下拉]    [上一月] [2026年 下拉] [5月 下拉] [下一月]    [今天]
```

### 3.2 控件尺寸

| 控件 | 宽度 | 高度 | 圆角 |
|---|---:|---:|---:|
| 工作日模式下拉 | 160 px | 44 px | 12 px |
| 年份下拉 | 120 px | 44 px | 12 px |
| 月份下拉 | 100 px | 44 px | 12 px |
| 今天按钮 | 96 px | 44 px | 12 px |
| 上一月和下一月按钮 | 36 px | 36 px | 10 px |

### 3.3 文案

| 控件 | 文案 |
|---|---|
| 工作日模式 | 工作日模式、双休、单休、大小周、自定义 |
| 年份 | 2026年 |
| 月份 | 5月 |
| 今天按钮 | 今天 |

### 3.4 状态

按钮需要支持以下状态：

1. 默认。
2. Hover。
3. Pressed。
4. Disabled。

## 4. 星期栏设计

### 4.1 星期显示

星期栏高度建议为 52 px。

显示为：

```text
一  二  三  四  五  六  日
```

### 4.2 周末颜色

周六、周日使用主题强调色。

绿色主题：

1. 周一至周五使用 #6B7280。
2. 周六周日使用 #168A45。

粉色主题：

1. 周一至周五使用 #6B7280。
2. 周六周日使用 #EF3D61。

## 5. 日历网格设计

### 5.1 网格结构

单月视图为 7 列 6 行。

每个日期格子建议尺寸：

| 模式 | 单元格宽度 | 单元格高度 |
|---|---:|---:|
| 单月大面板 | 96 px | 88 px |
| 单月紧凑面板 | 56 px | 64 px |
| 双月面板 | 64 px | 72 px |

### 5.2 日期格内容结构

每个日期格子内部结构：

```text
┌────────────────┐
│        休       │ 右上角标记，可选
│      2         │ 公历日期，大号
│      十六      │ 农历日期，小号
└────────────────┘
```

包含元素：

1. 公历日期数字。
2. 农历日期或节日名称。
3. 休息标记“休”。
4. 补班标记“班”。
5. 今日状态。
6. 选中状态。
7. 非本月弱化状态。

### 5.3 公历数字样式

| 类型 | 字号 | 字重 |
|---|---:|---|
| 普通日期 | 32 px | Semibold |
| 紧凑日期 | 22 px | Semibold |
| 非本月日期 | 30 px | Semibold |
| 选中日期 | 34 px | Bold |

### 5.4 农历文字样式

| 类型 | 字号 | 字重 |
|---|---:|---|
| 普通农历 | 18 px | Regular |
| 紧凑农历 | 12 px | Regular |
| 节日名称 | 16 px | Medium |
| 非本月农历 | 16 px | Regular |

### 5.5 农历显示规则

1. 公历数字居中偏上。
2. 农历文字位于公历数字下方。
3. 农历文字单行显示。
4. 文字过长时显示节日简称。
5. 农历初一显示月份名加初一，例如“五月初一”。
6. 节气和传统节日可以替代农历文本。
7. 节日优先级高于普通农历日期。

### 5.6 休息标记

休息标记显示在日期格右上角。

样式：

| 属性 | 数值 |
|---|---:|
| 宽度 | 24 px |
| 高度 | 24 px |
| 圆角 | 7 px |
| 字号 | 15 px |
| 字重 | Bold |

绿色主题：

```text
背景 #1E8E3E
文字 #FFFFFF
```

粉色主题：

```text
背景 #F04F7A
文字 #FFFFFF
```

### 5.7 补班标记

补班标记显示在日期格右上角，文案为“班”。

样式：

```text
背景 #526078
文字 #FFFFFF
字号 15 px
圆角 7 px
```

补班标记建议使用蓝灰色，不随主题大幅变化，避免和休息标记混淆。

## 6. 日期状态设计

### 6.1 普通工作日

普通工作日背景透明或白色。

文字：

```text
公历日期 #2D2F33
农历日期 #4B5563
```

### 6.2 普通周末

周六周日的公历日期使用主题色。

绿色主题：

```text
公历日期 #168A45
农历日期 #2D2F33
```

粉色主题：

```text
公历日期 #EF3D61
农历日期 #2D2F33
```

### 6.3 法定节假日

法定节假日需要有淡色背景和“休”标记。

绿色主题：

```text
日期背景 #ECF8F0
公历日期 #168A45
农历或节日文字 #168A45
休标记背景 #1E8E3E
```

粉色主题：

```text
日期背景 #FFF0F5
公历日期 #EF3D61
农历或节日文字 #EF3D61
休标记背景 #F04F7A
```

### 6.4 调休工作日

调休工作日显示“班”标记。

```text
日期背景 #FFFFFF
公历日期 #2D2F33
农历文字 #4B5563
班标记背景 #526078
班标记文字 #FFFFFF
```

### 6.5 今日

今日需要用描边突出。

绿色主题：

```text
描边 #277A46
描边宽度 2 px
背景 #F7FFF9
```

粉色主题：

```text
描边 #F04F7A
描边宽度 2 px
背景 #FFF7FA
```

### 6.6 选中起点和终点

起点和终点使用强描边或实色圆角块。

绿色主题：

```text
背景 #E8F8EE
描边 #168A45
描边宽度 2 px
圆角 14 px
```

粉色主题：

```text
背景 #FFF0F5
描边 #F04F7A
描边宽度 2 px
圆角 14 px
```

### 6.7 选中区间

区间中间日期使用淡色背景。

绿色主题：

```text
背景 #EDF8F1
```

粉色主题：

```text
背景 #FFF3F7
```

### 6.8 非本月日期

非本月日期需要弱化。

```text
公历日期 #B9BDC5
农历日期 #C7CBD1
背景透明
```

如果非本月日期属于周末，则可以使用淡主题色，透明度约 35%。

## 7. 底部统计区设计

### 7.1 结构

底部统计区高度建议为 108 px 至 132 px。

布局：

```text
┌──────────────────────────────────────────────┐
│ 已选择日期范围                               │
│ 5月2日（周六） 至 5月31日（周日）             │
│ 共 30 天                                     │
│                                              │
│ [总天数 31天] [工作日 22天] [休息日 9天]      │
└──────────────────────────────────────────────┘
```

如果使用粉色可爱主题，可以在左侧放猫咪装饰图。

### 7.2 统计卡片

每个统计卡片包含：

1. 图标。
2. 标题。
3. 数字。
4. 单位“天”。

卡片尺寸：

| 属性 | 数值 |
|---|---:|
| 宽度 | 112 px |
| 高度 | 92 px |
| 圆角 | 14 px |
| 内边距 | 12 px |
| 描边 | 1 px |

### 7.3 统计项

需要显示：

1. 总天数。
2. 工作日。
3. 休息日。
4. 法定节假日。
5. 调休工作日。

默认展示前三项，更多项可以在展开状态显示。

### 7.4 文案示例

```text
已选择日期范围
5月2日（周六） 至 5月31日（周日）
共 30 天
```

统计卡片：

```text
总天数 31 天
工作日 22 天
休息日 9 天
```

如果启用完整统计：

```text
法定节假日 3 天
调休工作日 1 天
```

## 8. 绿色清新主题

### 8.1 设计风格

关键词：

```text
清新、轻量、办公、干净、薄荷绿、柔和白色
```

### 8.2 色彩 Token

```text
primary: #2F9D5B
primaryDeep: #168A45
primaryLight: #EAF8F0
primarySoft: #F4FBF7
background: #FBFFFC
panelBackground: #FFFFFF
toolbarBackground: #F3FBF6
border: #CFE8D8
textPrimary: #2D2F33
textSecondary: #4B5563
textTertiary: #9CA3AF
weekend: #168A45
holidayTag: #1E8E3E
workdayTag: #526078
selectionBackground: #EAF8F0
selectionBorder: #168A45
shadow: rgba(0, 0, 0, 0.14)
```

### 8.3 细节

1. 顶部工具栏使用浅绿色渐变或浅绿色背景。
2. 面板描边为淡绿色。
3. 周六周日日期数字使用绿色。
4. 休息标记使用深绿色。
5. 选中日期使用绿色描边。
6. 底部统计区使用浅绿色卡片背景。
7. 图标可以使用公文包、日历、咖啡杯等办公感图标。

## 9. 粉色可爱主题

### 9.1 设计风格

关键词：

```text
柔和、可爱、奶油粉、小猫、办公小物、轻量、温暖
```

### 9.2 色彩 Token

```text
primary: #F04F7A
primaryDeep: #D83F68
primaryLight: #FFF0F5
primarySoft: #FFF7FA
background: #FFFBFD
panelBackground: #FFFFFF
toolbarBackground: #FFF2F6
border: #F6CEDB
textPrimary: #2D2F33
textSecondary: #5F6368
textTertiary: #B9BDC5
weekend: #EF3D61
holidayTag: #F04F7A
workdayTag: #526078
selectionBackground: #FFF0F5
selectionBorder: #F04F7A
shadow: rgba(0, 0, 0, 0.14)
```

### 9.3 可爱装饰

粉色主题可以使用：

1. 白色小猫装饰图。
2. 猫爪小图标。
3. 小爱心。
4. 粉色日历图标。
5. 柔和卡片阴影。

注意：

1. 装饰面积不能影响日期阅读。
2. 猫咪图片建议放在底部统计区左侧。
3. 猫咪图片不建议放到日历格子内。
4. 动画需要克制，可以只在复制成功时播放轻微动效。

## 10. 猫咪插图使用规范

### 10.1 使用位置

推荐放在：

1. 粉色主题底部统计区左侧。
2. 空状态页面。
3. 复制成功提示。
4. 关于页面。
5. 主题预览页。

### 10.2 图片要求

1. 白色小猫。
2. 粉色装饰。
3. 透明背景优先。
4. 如果用于抠图，可以提供黑色背景版本。
5. 画面主体居中。
6. 不要带复杂背景。
7. 边缘要清晰。
8. 尺寸建议 1024 px 或更高。

### 10.3 在底部统计区中的尺寸

| 属性 | 数值 |
|---|---:|
| 显示宽度 | 110 px |
| 显示高度 | 90 px |
| 左侧边距 | 16 px |
| 底部对齐 | 统计区底部 |

## 11. 设置页 UI

### 11.1 打开方式

设置入口放在顶部工具栏右侧或状态栏菜单中。

入口图标：

```text
gearshape
```

### 11.2 设置页结构

```text
设置
├─ 外观
│  ├─ 主题
│  ├─ 默认视图
│  └─ 状态栏图标
├─ 日历
│  ├─ 农历显示
│  ├─ 中国放假提示
│  ├─ 起始星期
│  └─ 日期格式
├─ 工作规则
│  ├─ 双休
│  ├─ 单休
│  ├─ 大小周
│  └─ 自定义每周规则
├─ 复制
│  ├─ 默认复制模板
│  └─ 自定义模板
└─ 数据
   ├─ 节假日数据
   ├─ 导入 JSON
   └─ 清空历史记录
```

### 11.3 设置控件

| 类型 | 控件 |
|---|---|
| 开关 | Toggle |
| 单选 | Picker segmented |
| 下拉 | Menu Picker |
| 日期选择 | DatePicker |
| 文本输入 | TextField |
| 文件导入 | FileImporter |
| 危险操作 | 红色按钮 |

## 12. 历史记录 UI

### 12.1 入口

可以在底部统计区增加历史按钮，也可以放在设置菜单中。

### 12.2 列表项

每条历史记录展示：

```text
5月2日 至 5月31日
共 30 天，工作日 22 天，休息日 8 天
2026.07.02 14:00
```

右侧操作：

1. 恢复选择。
2. 复制。
3. 删除。

## 13. 复制成功提示

复制后显示轻提示。

绿色主题：

```text
已复制到剪贴板
```

粉色主题：

```text
已复制到剪贴板
```

粉色主题可以附带小猫爪图标。

轻提示位置：

1. 面板底部中间。
2. 显示 1.5 秒后消失。
3. 背景使用深色半透明。
4. 文字使用白色。

## 14. 动效设计

动效需要短、轻、明确。

| 场景 | 动效 |
|---|---|
| 打开面板 | 透明度 0 到 1，轻微向下位移 |
| 关闭面板 | 透明度 1 到 0 |
| Hover 日期 | 背景淡入 |
| 选中日期 | 描边和背景淡入 |
| 统计数字变化 | 轻微数值过渡 |
| 复制成功 | Toast 淡入淡出 |

动画时长：

```text
普通交互 120 ms
面板打开 160 ms
Toast 1500 ms
```

## 15. 响应式规则

### 15.1 单月紧凑模式

当面板宽度小于 520 px：

1. 农历字体缩小。
2. 统计卡片改为一行紧凑文本。
3. 猫咪插图隐藏。
4. 只显示总天数、工作日、休息日。
5. 更多统计项进入展开区域。

### 15.2 大面板模式

当面板宽度大于 720 px：

1. 显示完整统计卡片。
2. 显示猫咪装饰图。
3. 显示更多统计项。
4. 支持双月或三月视图。

## 16. 图标建议

### 16.1 状态栏图标

需要支持模板图标，适配深色模式和浅色模式。

图标方案：

| 主题 | 图标 |
|---|---|
| 绿色主题 | 日历加叶子 |
| 粉色主题 | 日历加猫爪 |
| 通用 | 简洁日历图标 |

### 16.2 功能图标

| 功能 | SF Symbols 建议 |
|---|---|
| 今天 | calendar |
| 工作日 | briefcase.fill |
| 休息日 | cup.and.saucer.fill |
| 总天数 | calendar.badge.clock |
| 设置 | gearshape.fill |
| 复制 | doc.on.doc |
| 历史 | clock.arrow.circlepath |
| 上一月 | chevron.left |
| 下一月 | chevron.right |

## 17. SwiftUI 组件拆分建议

```text
StatusBarController
MainCalendarPopover
CalendarToolbarView
WeekdayHeaderView
MonthCalendarView
CalendarDayCell
DateRangeSummaryView
StatsCardView
ThemePickerView
SettingsView
HolidayDataView
WorkRuleSettingsView
CopyTemplateView
HistoryListView
ToastView
```

## 18. 组件职责

### 18.1 CalendarDayCell

负责显示：

1. 公历日期。
2. 农历文字。
3. 节日文字。
4. 休或班标记。
5. 今日样式。
6. 选中状态。
7. 非本月弱化状态。

### 18.2 MonthCalendarView

负责显示：

1. 一个完整月份。
2. 星期头。
3. 6 行 7 列日期格。
4. 鼠标拖拽事件。
5. 点击选择事件。

### 18.3 DateRangeSummaryView

负责显示：

1. 已选日期范围。
2. 总天数。
3. 工作日。
4. 休息日。
5. 法定节假日。
6. 调休工作日。
7. 复制按钮。

### 18.4 SettingsView

负责显示和修改：

1. 主题。
2. 默认视图。
3. 农历显示。
4. 中国放假提示。
5. 工作规则。
6. 复制模板。
7. 历史记录配置。

## 19. 开发提示词建议

可以把下面这段直接给代码生成 AI：

```text
请使用 SwiftUI 开发一个 macOS 菜单栏 App。App 常驻状态栏，点击菜单栏图标后弹出日历面板。面板支持单月、双月、三月视图，默认显示单月或双月。日历格子需要显示公历日期、农历日期、节日名称、休息标记“休”、补班标记“班”。用户可以通过鼠标拖拽框选日期范围，也可以点击开始日期和结束日期选择范围。选择后需要实时计算总天数、工作日、休息日、法定节假日、调休工作日。总天数包含首尾日期。工作日计算需要支持双休、单休、大小周、自定义每周规则，并且中国节假日和调休数据优先于普通周末规则。界面需要支持绿色清新主题和粉色可爱主题。粉色主题底部统计区可以显示白色小猫插图。所有设置、历史记录、节假日配置和自定义日期需要本地保存。请按组件拆分代码，包含 StatusBarController、MainCalendarPopover、CalendarToolbarView、MonthCalendarView、CalendarDayCell、DateRangeSummaryView、SettingsView、HolidayDataView、WorkRuleSettingsView、HistoryListView。
```

## 20. 验收标准

1. UI 视觉接近 macOS 原生风格。
2. 状态栏点击后面板位置正确，顶部小三角指向状态栏图标。
3. 顶部工具栏、星期栏、日历网格、统计区层级清晰。
4. 日期格能同时显示公历和农历。
5. 法定节假日有“休”标记。
6. 调休工作日有“班”标记。
7. 周末日期颜色和工作日不同。
8. 非本月日期弱化显示。
9. 今日日期有描边。
10. 选中范围有连续高亮。
11. 起点和终点有更强视觉标识。
12. 绿色主题清新轻量。
13. 粉色主题可爱但不影响阅读。
14. 底部统计区能清楚展示日期范围和统计数字。
15. 复制成功有轻提示。
